tasks:
  #Run all tasks:
    #shell: python -m robot --report NONE --outputdir output --logtitle "Task log" tasks.robot

  numberofarguments:
    robotTaskName: Call keywords with a varying number of arguments
  
  returnsavalue:
    robotTaskName:  Call a keyword that returns a value
  
  IFELSEexecution:
    robotTaskName: Do conditional IF - ELSE IF - ELSE execution

  Loopalist:
    robotTaskName: Loop a list

  Loopadictionary:
    robotTaskName: Loop a dictionary
  
  toendindex:
    robotTaskName: Loop a range from 0 to end index

  


condaConfigFile: conda.yaml
artifactsDir: output
PATH:
  - .
PYTHONPATH:
  - .
ignoreFiles:
  - .gitignore
